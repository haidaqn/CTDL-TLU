#include <iostream>
#include "stack.h"
#include "queue.h"
#include "list.h"
#include "tree.h"

using namespace std;

int main() {
    Tree<int> q;

    q.ADD(1);
    q.ADD(5);
    q.ADD(9);
    q.ADD(10);
    q.ADD(2);

   

    return 0;
}